# Archives
application/bzip2; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/gzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.android.package-archive; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-cab-compressed; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.debian.binary-package; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.rar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-7z-compressed; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-7z-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ace; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-alz; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-apple-diskimage; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-archive; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-arj; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-brotli; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip-brotli-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip1; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip1-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip3; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzip3-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cabinet; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cd-image; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-compress; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cpio; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-chrome-extension; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-deb; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ear; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ms-dos-executable; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-gtar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-gtar; /usr/bin/tar tvf %s; copiousoutput
application/x-gzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-java-archive; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lha; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lhz; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lrzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lrzip-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lz4; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lz4-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lzip-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lzma; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lzma-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-lzop; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ms-wim; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-rar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-rar-compressed; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-rpm; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-source-rpm; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-rzip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-rzip-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-stuffit; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-tar; /usr/bin/tar tvf %s; copiousoutput
application/x-tarz; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-tzo; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ustar; /usr/bin/tar tvf %s; copiousoutput
application/x-war; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-xar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-xz; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-xz-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-zip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-zip-compressed; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-zstd-compressed-tar; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-zoo; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/zip; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/zip; /usr/bin/zipinfo -m -- %s; copiousoutput
application/zstd; /usr/bin/file-roller -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"


# Documents
application/pdf; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-pdf; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzpdf; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-gzpdf; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-xzpdf; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

image/vnd.djvu; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-djv; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-djvu; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/postscript; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-ps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzpostscript; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-gzpostscript; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

image/x-eps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-eps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
image/x-bzeps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
image/x-gzeps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/x-dvi; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-dvi; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-bzdvi; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-gzdvi; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.comicbook-rar; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.comicbook+zip; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cb7; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-cb7; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cbr; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-cbr; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cbt; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-cbt; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-cbz; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ext-cbz; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

image/tiff; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/illustrator; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/oxps; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-xpsdocument; /usr/bin/evince -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/ebook+zip; /usr/bin/foliate -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-mobipocket-ebook; /usr/bin/foliate -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.amazon.mobi8-ebook; /usr/bin/foliate -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-fictionbook+xml; /usr/bin/foliate -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.oasis.opendocument.text; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.text-*; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.openxmlformats-officedocument.wordprocessingml.*; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/msword; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-word; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-word.*; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-works; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/rtf; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
text/rtf; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.wordperfect; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/wordperfect; /usr/bin/soffice --nologo --writer %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.oasis.opendocument.spreadsheet; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.spreadsheet-*; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.chart; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.chart-*; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.openxmlformats-officedocument.spreadsheetml.*; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/excel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/msexcel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-excel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-excel.*; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-dos_ms_excel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-excel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ms-excel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-msexcel; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
text/spreadsheet; /usr/bin/soffice --nologo --calc %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.oasis.opendocument.presentation; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.presentation-*; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.openxmlformats-officedocument.presentationml.*; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/mspowerpoint; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-powerpoint; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-powerpoint.*; /usr/bin/soffice --nologo --impress %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.oasis.opendocument.graphics; /usr/bin/soffice --nologo --draw %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.graphics-*; /usr/bin/soffice --nologo --draw %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.corel-draw; /usr/bin/soffice --nologo --draw %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-publisher; /usr/bin/soffice --nologo --draw %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.visio; /usr/bin/soffice --nologo --draw %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"

application/vnd.oasis.opendocument.formula; /usr/bin/soffice --nologo --math %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.oasis.opendocument.formula-*; /usr/bin/soffice --nologo --math %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/mathml; /usr/bin/soffice --nologo --math %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/mathml+xml; /usr/bin/soffice --nologo --math %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"


# Text Files
application/ecmascript; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/javascript; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/json; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/xml; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-awk; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-cdrdao-toc; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-cue; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-perl; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-php; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-ruby; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-shellscript; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-subrip; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
application/x-yaml; /usr/bin/vim -R -n -i NONE -- %s; needsterminal
text/html; /usr/bin/firefox -new-tab %s & sleep 10; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
text/html; /usr/bin/links %s; needsterminal
text/html; /usr/bin/links -codepage utf-8 -dump %s; copiousoutput
text/*; /usr/bin/vim -R -n -i NONE -- %s; needsterminal


# Fonts
font/otf; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
font/ttf; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
font/woff; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
font/ttc; /usr/libexec/font-manager/font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
font/sfnt; /usr/libexec/font-manager/font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-font-otf; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-font-ttf; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-font-type1; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-font-pcf; /usr/bin/gnome-font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/vnd.ms-opentype; /usr/libexec/font-manager/font-viewer -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"


# Images
image/*; /usr/bin/loupe -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
# image/*; /usr/bin/display-im7 -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"


# Multimedia
audio/*; /usr/bin/mpv --player-operation-mode=pseudo-gui -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
audio/*; /usr/bin/mpv -- %s; needsterminal
application/ogg; /usr/bin/mpv --player-operation-mode=pseudo-gui -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/ogg; /usr/bin/mpv -- %s; needsterminal
application/x-ogg; /usr/bin/mpv --player-operation-mode=pseudo-gui -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
application/x-ogg; /usr/bin/mpv -- %s; needsterminal
video/*; /usr/bin/mpv --player-operation-mode=pseudo-gui -- %s; test=test "$DISPLAY" -o "$WAYLAND_DISPLAY"
